<div class="jumbotron">
  <h1><%= business.name %></h1>
  <h4>Businesses don't currently have addresses</h4>
  <a class="btn btn-primary" href="mailto:<%= business.account.email %>">
    <span class="glyphicon glyphicon-envelope">&nbsp;Email</span>
  </a>
  <%= link_to business, class:"btn btn-primary" do %>
    <span class="glyphicon glyphicon-home"></span>
  <% end %>
</div>
