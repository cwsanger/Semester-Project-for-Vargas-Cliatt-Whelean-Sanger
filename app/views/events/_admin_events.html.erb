<div class="input-group"> <span class="input-group-addon"><i class='glyphicon glyphicon-search'></i></span>
   <input id="filter" type="text" class="form-control" placeholder="Find events...">
</div>
<table class="table table-striped table-hover table-bordered">
  <thead>
    <tr>
      <th><center>Description</center></th>
      <th><center>Delete</center></th>
    </tr>
  </thead>
	  <tbody class="searchable">
	   <% @events.each do |event| %>
	      <tr>
	        <td><%= event.body %></td>
	        <td><center><%= link_to "", neighborhood_event_url(@current_member.neighborhood, event), method: :delete,class:"glyphicon glyphicon-minus" %></center></td>
	      </tr>
	    <% end %>
	  </tbody>
</table>
<script>
  $(document).ready(function () {
    (function ($) {
        $('#filter').keyup(function () {
            var rex = new RegExp($(this).val(), 'i');
            $('.searchable tr').hide();
            $('.searchable tr').filter(function () {
                return rex.test($(this).text());
            }).show();
        })
    }($));
});
</script>
