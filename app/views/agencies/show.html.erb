<%= render partial: 'navbar' %>
<br><br><br><br><br><br>
<%= image_tag(@current_member.image_url, :width => 80) if @current_member.image_url.present? %>
<%= button_to 'Edit agency details',
    edit_agency_path(@current_member), class: 'btn linky', method: :get %>

<div class="row">
  <div class="col-md-2 col-md-offset-1">
    <%= render 'alerts/form' %>
  </div>
  <div class="col-md-6">
      <%= render partial: 'alerts/agencyAlert' %>
  </div>
</div>
